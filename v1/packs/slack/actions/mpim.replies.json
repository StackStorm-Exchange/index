{
    "description": "This method returns an entire thread (a message plus all the messages in reply to it).",
    "enabled": true,
    "entry_point": "run.py",
    "name": "mpim.replies",
    "parameters": {
        "channel": {
            "required": true,
            "type": "string"
        },
        "end_point": {
            "default": "mpim.replies",
            "immutable": true,
            "type": "string"
        },
        "thread_ts": {
            "required": true,
            "type": "string"
        },
        "token": {
            "required": false,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

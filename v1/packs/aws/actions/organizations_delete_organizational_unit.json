{
    "description": null,
    "enabled": true,
    "entry_point": "run.py",
    "name": "organizations_delete_organizational_unit",
    "pack": "aws",
    "parameters": {
        "OrganizationalUnitId": {
            "description": "The unique identifier (ID) of the organizational unit that you want to delete. You can get the ID from the ListOrganizationalUnitsForParent operation. The regex pattern for an organizational unit ID string requires 'ou-' followed by from 4 to 32 lower-case letters or digits (the ID of the root that contains the OU) followed by a second '-' dash and from 8 to 32 additional lower-case letters or digits.",
            "required": true,
            "type": "string"
        },
        "action": {
            "default": "delete_organizational_unit",
            "immutable": true,
            "type": "string"
        },
        "cls": {
            "default": "organizations",
            "type": "string"
        },
        "headers": {
            "type": "string"
        },
        "module_path": {
            "default": "boto3",
            "immutable": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

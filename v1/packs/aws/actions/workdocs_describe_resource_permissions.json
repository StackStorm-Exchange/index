{
    "description": null,
    "enabled": true,
    "entry_point": "run.py",
    "name": "workdocs_describe_resource_permissions",
    "pack": "aws",
    "parameters": {
        "AuthenticationToken": {
            "description": "Amazon WorkDocs authentication token. This field should not be set when using administrative API actions, as in accessing the API using AWS credentials.",
            "type": "string"
        },
        "Limit": {
            "description": "The maximum number of items to return with this call.",
            "type": "integer"
        },
        "Marker": {
            "description": "The marker for the next set of results. (You received this marker from a previous call)",
            "type": "string"
        },
        "ResourceId": {
            "description": "The ID of the resource.",
            "required": true,
            "type": "string"
        },
        "action": {
            "default": "describe_resource_permissions",
            "immutable": true,
            "type": "string"
        },
        "cls": {
            "default": "workdocs",
            "type": "string"
        },
        "headers": {
            "type": "string"
        },
        "module_path": {
            "default": "boto3",
            "immutable": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

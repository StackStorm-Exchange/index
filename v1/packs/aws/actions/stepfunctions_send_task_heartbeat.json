{
    "description": null,
    "enabled": true,
    "entry_point": "run.py",
    "name": "stepfunctions_send_task_heartbeat",
    "pack": "aws",
    "parameters": {
        "action": {
            "default": "send_task_heartbeat",
            "immutable": true,
            "type": "string"
        },
        "cls": {
            "default": "stepfunctions",
            "type": "string"
        },
        "headers": {
            "type": "string"
        },
        "module_path": {
            "default": "boto3",
            "immutable": true,
            "type": "string"
        },
        "taskToken": {
            "description": "The token that represents this task. Task tokens are generated by the service when the tasks are assigned to a worker (see GetActivityTask::taskToken).",
            "required": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

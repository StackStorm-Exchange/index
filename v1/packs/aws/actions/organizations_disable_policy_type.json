{
    "description": null,
    "enabled": true,
    "entry_point": "run.py",
    "name": "organizations_disable_policy_type",
    "pack": "aws",
    "parameters": {
        "PolicyType": {
            "description": "The policy type that you want to disable in this root.",
            "required": true,
            "type": "string"
        },
        "RootId": {
            "description": "The unique identifier (ID) of the root in which you want to disable a policy type. You can get the ID from the ListPolicies operation. The regex pattern for a root ID string requires 'r-' followed by from 4 to 32 lower-case letters or digits.",
            "required": true,
            "type": "string"
        },
        "action": {
            "default": "disable_policy_type",
            "immutable": true,
            "type": "string"
        },
        "cls": {
            "default": "organizations",
            "type": "string"
        },
        "headers": {
            "type": "string"
        },
        "module_path": {
            "default": "boto3",
            "immutable": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

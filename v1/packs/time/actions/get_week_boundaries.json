{
    "description": "Retrieve week boundary timestamps (week start and end) for the provided date.",
    "enabled": true,
    "entry_point": "get_week_boundaries.py",
    "name": "get_week_boundaries",
    "parameters": {
        "date": {
            "description": "Date string to retrieve the boundaries for. If not provided it defaults to the current date.",
            "required": false,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

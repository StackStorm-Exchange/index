{
    "description": "Add a template combination (resource: POST /api/environments/:environment_id/template_combinations)",
    "enabled": true,
    "entry_point": "lib/action.py",
    "name": "environments.template_combinations_create",
    "pack": "foreman",
    "parameters": {
        "config_template_id": {
            "description": "ID of config template",
            "required": false,
            "type": "string"
        },
        "connection": {
            "description": "Name of <connection> from this pack's configuration that specifies how to connect to a server.",
            "required": false,
            "type": "string"
        },
        "environment_id": {
            "description": "ID of environment",
            "required": true,
            "type": "string"
        },
        "hostgroup_id": {
            "description": "ID of host group",
            "required": false,
            "type": "string"
        },
        "location_id": {
            "description": "Scope by locations",
            "required": false,
            "type": "number"
        },
        "operation": {
            "default": "environments.template_combinations_create",
            "immutable": true,
            "type": "string"
        },
        "organization_id": {
            "description": "Scope by organizations",
            "required": false,
            "type": "number"
        },
        "password": {
            "description": "Optional override of the password in <connection>. (required if <connection> is not specified)",
            "required": false,
            "secret": true,
            "type": "string"
        },
        "provisioning_template_id": {
            "description": "ID of config template",
            "required": false,
            "type": "string"
        },
        "server": {
            "description": "Optional override of the server in <connection> (required if <connection> is not specified).",
            "required": false,
            "type": "string"
        },
        "template_combination": {
            "description": "\"description: \n parameters:\n     environment_id:\n       required: false\n       type: number\n       description: environment id\n     hostgroup_id:\n       required: false\n       type: number\n       description: host group id\n     \"",
            "required": true,
            "type": "object"
        },
        "username": {
            "description": "Optional override of the username in <connection> (example: username@domain.tld) (required if <connection> is not specified).",
            "required": false,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

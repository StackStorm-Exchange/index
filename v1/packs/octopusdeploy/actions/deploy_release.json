{
    "description": "Deploy a release in Octopus.",
    "enabled": true,
    "entry_point": "deploy_release.py",
    "name": "deploy_release",
    "parameters": {
        "environment_id": {
            "description": "ID of the environment in Octopus",
            "required": true,
            "type": "string"
        },
        "release_id": {
            "description": "ID of the release in Octopus",
            "required": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

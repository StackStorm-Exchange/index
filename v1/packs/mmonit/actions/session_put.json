{
    "description": "Add or update the session attribute. If a named attribute already exist, its old value is replaced.",
    "enabled": true,
    "entry_point": "session_delete.py",
    "name": "session_put",
    "parameters": {
        "attribute": {
            "description": "The key/value of the session attribute to create/update in the following format key:value",
            "required": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

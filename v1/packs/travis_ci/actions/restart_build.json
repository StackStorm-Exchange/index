{
    "description": "Restart a Build",
    "enabled": true,
    "entry_point": "restart_build.py",
    "name": "restart_build",
    "parameters": {
        "buildid": {
            "description": "ID of the build to restart.",
            "required": true,
            "type": "integer"
        }
    },
    "runner_type": "python-script"
}

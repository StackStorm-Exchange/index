{
    "description": "vTM - Enable SSL Encryption for a Pool",
    "enabled": true,
    "entry_point": "vtm_enable_ssl_encryption.py",
    "name": "vtm_enable_ssl_encryption",
    "parameters": {
        "name": {
            "description": "The name of the Pool",
            "required": true,
            "type": "string"
        },
        "verify": {
            "default": false,
            "description": "Enable strict verification of the servers certificate",
            "required": false,
            "type": "boolean"
        },
        "vtm": {
            "description": "The vTM instance to configure, the tag or instance ID on BSD.",
            "required": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

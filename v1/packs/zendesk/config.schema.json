{
    "api_token": {
        "description": "API token generated in the Zendesk admin interface",
        "required": true,
        "secret": true,
        "type": "string"
    },
    "email": {
        "description": "Email of the account being used for the integration",
        "required": true,
        "secret": false,
        "type": "string"
    },
    "subdomain": {
        "description": "The subdomain of the Zendesk account",
        "required": true,
        "secret": false,
        "type": "string"
    }
}

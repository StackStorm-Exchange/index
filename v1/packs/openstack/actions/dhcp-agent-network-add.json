{
    "description": "Add a network to a DHCP agent.",
    "enabled": true,
    "entry_point": "src/wrapper.py",
    "name": "dhcp-agent-network-add",
    "parameters": {
        "base": {
            "default": "dhcp-agent-network-add",
            "immutable": true,
            "type": "string"
        },
        "cloud": {
            "description": "A specific cloud to query",
            "type": "string"
        },
        "dhcp_agent": {
            "description": "ID of the DHCP agent.",
            "required": true,
            "type": "string"
        },
        "ep": {
            "default": "EntryPoint.parse('dhcp-agent-network-add = neutronclient.neutron.v2_0.agentscheduler:AddNetworkToDhcpAgent')",
            "immutable": true,
            "type": "string"
        },
        "network": {
            "description": "Network to add.",
            "required": true,
            "type": "string"
        },
        "project_id": {
            "description": "Run the action under a different project, identified by id",
            "type": "string"
        },
        "project_name": {
            "description": "Run the action under a different project, identified by name",
            "type": "string"
        },
        "request_format": {
            "description": "==SUPPRESS== (choices: json)",
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

{
    "api_password": {
        "description": "API password for querying Icinga2 for objects",
        "required": true,
        "secret": true,
        "type": "string"
    },
    "api_state_change_password": {
        "description": "API password for querying API state changes",
        "required": true,
        "secret": true,
        "type": "string"
    },
    "api_state_change_user": {
        "description": "API user name for querying API state changes",
        "required": true,
        "secret": false,
        "type": "string"
    },
    "api_url": {
        "description": "URL to the API stream, e.g. 'https://localhost:5665/v1'",
        "required": true,
        "secret": false,
        "type": "string"
    },
    "api_user": {
        "description": "API user name to query Icinga2 host for objects",
        "required": true,
        "secret": false,
        "type": "string"
    }
}

{
    "description": "Retrieve a temporary session key to use for encrypting and decrypting secrets via the API.",
    "enabled": true,
    "entry_point": "run.py",
    "name": "post.secrets.get_session_key",
    "parameters": {
        "endpoint_uri": {
            "default": "/secrets/get-session-key/",
            "immutable": true,
            "type": "string"
        },
        "get_detail_route_eligible": {
            "default": false,
            "immutable": true,
            "type": "boolean"
        },
        "http_verb": {
            "default": "post",
            "immutable": true,
            "type": "string"
        },
        "private_key": {
            "description": "User's private key.",
            "required": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

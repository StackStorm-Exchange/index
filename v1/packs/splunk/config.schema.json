{
    "host": {
        "description": "Splunk server",
        "required": true,
        "secret": false,
        "type": "string"
    },
    "password": {
        "description": "Splunk password",
        "required": true,
        "secret": true,
        "type": "string"
    },
    "port": {
        "default": 8089,
        "description": "Splunk port (default 8089)",
        "required": true,
        "secret": false,
        "type": "integer"
    },
    "scheme": {
        "default": "https",
        "description": "Protocol used for Splunk API - http or https",
        "required": false,
        "secret": false,
        "type": "string"
    },
    "username": {
        "description": "Splunk username",
        "required": true,
        "secret": false,
        "type": "string"
    },
    "verify": {
        "default": true,
        "description": "Check the validity of the SSL certificate returned by Splunk",
        "required": false,
        "secret": false,
        "type": "boolean"
    }
}

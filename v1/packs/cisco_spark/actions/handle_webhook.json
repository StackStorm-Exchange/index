{
    "description": "Handle inbound webhook ChatOps request",
    "enabled": true,
    "entry_point": "workflows/handle_webhook.yaml",
    "name": "handle_webhook",
    "pack": "cisco_spark",
    "parameters": {
        "messageId": {
            "required": true,
            "type": "string"
        },
        "roomId": {
            "required": true,
            "type": "string"
        }
    },
    "runner_type": "orquesta"
}

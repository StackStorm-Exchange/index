{
    "api_key": {
        "description": "API Key for Softlayer integration",
        "required": true,
        "secret": true,
        "type": "string"
    },
    "region": {
        "description": "Not used yet, should be the default region you want to work with for instances",
        "required": false,
        "secret": false,
        "type": "string"
    },
    "swift_region": {
        "description": "Not used yet, will be the Softlayer Object Storage region to work with",
        "required": false,
        "secret": false,
        "type": "string"
    },
    "username": {
        "description": "Softlayer username",
        "required": true,
        "secret": false,
        "type": "string"
    }
}

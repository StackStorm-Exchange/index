{
    "description": "Deletes a keypair by name. If there are mutiple keys with the same name it will only delete the first",
    "enabled": true,
    "entry_point": "delete_keypair.py",
    "name": "delete_keypair",
    "parameters": {
        "name": {
            "description": "Name of the key pair.",
            "required": true,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}

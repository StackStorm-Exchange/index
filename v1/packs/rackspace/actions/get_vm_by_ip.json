{
    "description": "Retrieve information for a VM which matches the provided public IP.",
    "enabled": true,
    "entry_point": "get_vm_by_ip.py",
    "name": "get_vm_by_ip",
    "parameters": {
        "ip_address": {
            "description": "IP address to filter on.",
            "required": true,
            "type": "string"
        },
        "region": {
            "description": "Optional region name. Defaults to region specified in the config.",
            "required": false,
            "type": "string"
        }
    },
    "runner_type": "python-script"
}
